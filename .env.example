# ============================================================
# XPERT.IA — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores reais
# NUNCA commite o .env com valores reais!
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
SUPABASE_URL=https://SEU_PROJECT_ID.supabase.co
SUPABASE_ANON_KEY=eyJhbGci...   # Project Settings → API → anon key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...  # Project Settings → API → service_role key (secreto!)

# ── Evolution API ─────────────────────────────────────────────
EVOLUTION_API_URL=https://sua-evolutionapi.seudominio.com
EVOLUTION_API_KEY=SUA_CHAVE_GLOBAL_AQUI

# ── OpenAI ───────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-...

# ── SerpAPI (busca na web) ───────────────────────────────────
SERPAPI_KEY=SUA_CHAVE_SERPAPI

# ── n8n ──────────────────────────────────────────────────────
N8N_URL=https://seu-n8n.seudominio.com
N8N_API_KEY=SUA_API_KEY_N8N   # Settings → n8n API

# ── EasyPanel (deploy) ───────────────────────────────────────
# Não requer variáveis de ambiente — build via Dockerfile em /software
# Configurar no EasyPanel: Source = GitHub repo, Build path = /software

# ── Credenciais padrão do admin ──────────────────────────────
# Crie manualmente no Supabase Authentication → Users → Add User
# Depois atualize o role para 'admin' em:
# UPDATE profiles SET role = 'admin' WHERE id = 'uuid-do-admin';
ADMIN_EMAIL=seuemail@dominio.com
ADMIN_PASSWORD=senha-forte-aqui
